import { FileSizeFormatPipePipe } from './file-size-format-pipe.pipe';

describe('FileSizeFormatPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FileSizeFormatPipePipe();
    expect(pipe).toBeTruthy();
  });
});
